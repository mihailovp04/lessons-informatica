# Конспект-лекция по теме §1.3. Процедуры (Pascal)

## 1. Определение и форма записи
Процедура в языке Паскаль описывается так:
```pascal
procedure p(x1, x2, ..., xn);
  D;
begin
  ...
end;
```

- **p** – имя процедуры;  
- **(x1, x2, …, xn)** – список формальных параметров;  
- **D** – локальные описания (как у функций);  
- **begin … end** – тело процедуры, где имя процедуры не используется в присваиваниях.

---

## 2. Возврат результатов
- Функция возвращает **одно значение** через своё имя.  
- Процедура может возвращать **несколько результатов**.  
- Делается это через параметры, объявленные со словом `var` в списке формальных параметров.

---

## 3. Виды формальных параметров

### 3.1. Параметры-значения
Форма:
```pascal
v1, v2, ..., vk : тип
```
- Используются для передачи значений в процедуру.  
- В качестве фактического параметра можно указать:  
  - константу,  
  - переменную,  
  - выражение.  
- Изменения таких параметров **не сохраняются** во внешней программе.

---

### 3.2. Параметры-переменные
Форма:
```pascal
var v1, v2, ..., vk : тип
```
- Используются для возврата результатов из процедуры.  
- Фактическим параметром может быть только **переменная**.  
- Все изменения сохраняются во внешней программе.

---

## 4. Вызов процедуры
Записывается так:
```pascal
p(a1, a2, ..., an)
```

- `(a1, a2, …, an)` – список фактических параметров.  
- В отличие от функции, вызов процедуры — это **оператор**.  
- Операторы вызова вставляются в программу там, где нужно выполнить обработку данных.

---

## 5. Правила передачи параметров
- Для параметра-значения: можно использовать любое выражение совместимого типа (в том числе константу).  
- Для параметра-переменной: фактическим параметром может быть только переменная; изменения сохраняются во внешней программе.

---

## 6. Пример (Program P99)
```pascal
Program P99;
{Описание и использование процедуры Lac}
var a, b, c, t, q : real;

procedure Lac(r : real; var l, s : real);
{Длина окружности и площадь круга}
{r - радиус; l - длина; s - площадь}
const Pi = 3.14159;
begin
  l := 2 * Pi * r;
  s := Pi * sqr(r);
end; {Lac}

begin
  a := 1.0;
  Lac(a, b, c);
  writeln(a:10:5, b:10:5, c:10:5);
  Lac(3.0, t, q);
  writeln(3.0:10:5, t:10:5, q:10:5);
  readln;
end.
```

### Разбор примера
- `r` – параметр-значение (можно передавать константу или выражение).  
- `l, s` – параметры-переменные (результаты вычислений сохраняются во внешних переменных).  

**Вызов 1:**  
```pascal
a := 1.0;
Lac(a, b, c);
```
Эквивалентно:
```pascal
b := 2*Pi*1.0;
c := Pi*sqr(1.0);
```

**Вызов 2:**  
```pascal
Lac(3.0, t, q);
```
Эквивалентно:
```pascal
t := 2*Pi*3.0;
q := Pi*sqr(3.0);
```

---
# Вопросы и упражнения (из книги)

## 🔹 Задание 1
> В чём разница между параметром-значением и параметром-переменной?

**Пояснение:**  
- Параметр-значение: изменения не выходят наружу.  
- Параметр-переменная: изменения сохраняются.  

---

## 🔹 Задание 2
> Даны описания:  
> ```pascal
> var k, m, n : integer;  
>     a, b, c : real;  
>
> procedure P(i : integer; var j : integer; x : real; var y : real);  
> begin {...} end;
> ```  
> Определить, какие вызовы корректны.  

**Разбор вызовов:**  
1. `P(k, m, a, b)` ✅  
2. `P(3, m, a, b)` ✅  
3. `P(k, 3, a, b)` ❌ (нельзя константу в `var j`)  
4. `P(m, m, a, b)` ✅  
5. `P(m, k, 6.1, b)` ✅  
6. `P(n, m, 6, b)` ✅  
7. `P(m, k, 6.1, b)` ✅  
8. `P(a, m, b, c)` ❌ (несовпадение типов)  
9. `P(i, i, i, i)` ❌ (переменная i не объявлена)  
10. `P(a, a, a, a)` ❌ (тип real вместо integer)  

---

## 🔹 Задание 3
> Программа P100:

```pascal
Program P100;
var a : real; 
    b : integer;

procedure P(x : real; var y : integer);
begin
  {...}
end;

begin
  P(a, b);   { корректно }
  P(0.1, a); { ошибка: a real, ожидался integer }
  P(1, b);   { корректно }
  P(a, 1);   { ошибка: нельзя константу в var }
end.
```

**Пояснение:**  
- Ошибка №1: `P(0.1, a)` — тип не совпадает.  
- Ошибка №2: `P(a, 1)` — константа не может быть var.  

---

## 🔹 Задание 4
> Программа P101:

```pascal
Program P101;
var a, b : integer;

procedure P(x : integer; var y : integer);
begin
  x := x + 1;
  y := y + 1;
  writeln('x=', x, ' y=', y);
end;

begin
  a := 0;
  b := 0;
  P(a, b);
  writeln('a=', a, ' b=', b);
end.
```

**Разбор:**  
1. Вызов `P(a, b)` → передаём `a=0` (значение) и `b=0` (var).  
2. Внутри процедуры:  
   - `x := x + 1` → локально `x=1`.  
   - `y := y + 1` → реально `b=1`.  
   - Печать: `x=1 y=1`.  
3. После процедуры:  
   - `a=0` (не изменился),  
   - `b=1` (изменился).  
   - Печать: `a=0 b=1`.  

**Вывод программы:**
```
x=1 y=1
a=0 b=1
```

---

# Итоговые выводы
- Процедуры — это подпрограммы, которые возвращают результаты через `var`.  
- Параметры-значения передают копию, параметры-переменные передают ссылку.  
- Разница отчётливо видна в программах P99–P101.  
