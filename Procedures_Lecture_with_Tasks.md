# Конспект-лекция по теме §1.3. Процедуры (Pascal)

## Теоретическая часть

### 1. Определение и форма записи
Процедура в языке Паскаль описывается так:
```pascal
procedure p(x1, x2, ..., xn);
  D;
begin
  ...
end;
```

- **p** – имя процедуры;  
- **(x1, x2, …, xn)** – список формальных параметров;  
- **D** – локальные описания (как у функций);  
- **begin … end** – тело процедуры, где имя процедуры не используется в присваиваниях.

---

### 2. Возврат результатов
- Функция возвращает **одно значение** через своё имя.  
- Процедура может возвращать **несколько результатов**.  
- Делается это через параметры, объявленные со словом `var` в списке формальных параметров.

---

### 3. Виды формальных параметров

#### 3.1. Параметры-значения
Форма:
```pascal
v1, v2, ..., vk : тип
```
- Используются для передачи значений в процедуру.  
- В качестве фактического параметра можно указать:  
  - константу,  
  - переменную,  
  - выражение.  
- Изменения таких параметров **не сохраняются** во внешней программе.

#### 3.2. Параметры-переменные
Форма:
```pascal
var v1, v2, ..., vk : тип
```
- Используются для возврата результатов из процедуры.  
- Фактическим параметром может быть только **переменная**.  
- Все изменения сохраняются во внешней программе.

---

### 4. Вызов процедуры
Записывается так:
```pascal
p(a1, a2, ..., an)
```

- `(a1, a2, …, an)` – список фактических параметров.  
- В отличие от функции, вызов процедуры — это **оператор**.  
- Операторы вызова вставляются в программу там, где нужно выполнить обработку данных.

---

### 5. Пример (Program P99)
```pascal
Program P99;
{Описание и использование процедуры Lac}
var a, b, c, t, q : real;

procedure Lac(r : real; var l, s : real);
{Длина окружности и площадь круга}
{r - радиус; l - длина; s - площадь}
const Pi = 3.14159;
begin
  l := 2 * Pi * r;
  s := Pi * sqr(r);
end; {Lac}

begin
  a := 1.0;
  Lac(a, b, c);
  writeln(a:10:5, b:10:5, c:10:5);
  Lac(3.0, t, q);
  writeln(3.0:10:5, t:10:5, q:10:5);
  readln;
end.
```

**Разбор примера:**  
- `r` – параметр-значение (копируется внутрь).  
- `l, s` – параметры-переменные (результаты возвращаются наружу).  

---

## Практическая часть (задания из учебника)

### 🔹 Задание 1
**Вопрос:** В чём разница между параметром-значением и параметром-переменной?  

**Ответ:**  
- **Параметр-значение** передаёт копию значения в процедуру, изменения не возвращаются.  
- **Параметр-переменная (var)** передаёт ссылку на переменную, и все изменения сохраняются.  

**Пример кода:**
```pascal
procedure P(x: integer; var y: integer);
begin
  x := x + 1;   { изменится только локальная копия }
  y := y + 1;   { изменится реальная переменная }
end;

var a, b: integer;
begin
  a := 5; b := 5;
  P(a, b);
  writeln('a=', a, ' b=', b);  { результат: a=5, b=6 }
end.
```

---

### 🔹 Задание 2
**Дано:**
```pascal
var k, m, n : integer;
    a, b, c : real;

procedure P(i : integer; var j : integer; x : real; var y : real);
begin
  {...}
end;
```

**Какие вызовы корректны?**  

1. `P(k, m, a, b)` ✅ — все параметры подходят по типу.  
2. `P(3, m, a, b)` ✅ — допустимо, так как `3` можно передать параметру-значению.  
3. `P(k, 3, a, b)` ❌ — нельзя передавать константу `3` на место `var j`.  
4. `P(m, m, a, b)` ✅ — допускается, хотя одна и та же переменная передана дважды.  
5. `P(m, k, 6.1, b)` ✅ — `6.1` корректен как параметр-значение.  
6. `P(n, m, 6, b)` ✅ — целое `6` совместимо с `real`.  
7. `P(m, k, 6.1, b)` ✅ — аналогично предыдущему.  
8. `P(a, m, b, c)` ❌ — `a` имеет тип `real`, а ожидается `integer`.  
9. `P(i, i, i, i)` ❌ — переменная `i` не объявлена, некорректно.  
10. `P(a, a, a, a)` ❌ — несовместимость типов (`real` вместо `integer`).  

---

### 🔹 Задание 3
**Программа P100 (с ошибками):**
```pascal
Program P100;
var a : real; 
    b : integer;

procedure P(x : real; var y : integer);
begin
  {...}
end;

begin
  P(a, b);   { корректно }
  P(0.1, a); { ошибка: a имеет тип real, а должен быть integer для var }
  P(1, b);   { корректно: 1 можно передать как real в x }
  P(a, 1);   { ошибка: константа не может быть параметром var }
end.
```

**Пояснение:**  
- Ошибка №1: `P(0.1, a)` — переменная `a` имеет тип `real`, а требуется `integer`.  
- Ошибка №2: `P(a, 1)` — константа `1` не может быть передана параметру-переменной.  

---

### 🔹 Задание 4
**Программа P101:**
```pascal
Program P101;
var a, b : integer;

procedure P(x : integer; var y : integer);
begin
  x := x + 1;
  y := y + 1;
  writeln('x=', x, ' y=', y);
end;

begin
  a := 0;
  b := 0;
  P(a, b);
  writeln('a=', a, ' b=', b);
end.
```

**Пошаговое выполнение:**  
1. Вызов `P(a, b)` → передаём `a=0` как значение, `b=0` как переменную.  
2. Внутри процедуры:  
   - `x := x + 1` → `x = 1` (локальная копия `a`).  
   - `y := y + 1` → `y = 1` (переменная `b` изменяется реально).  
   - Печать: `x=1 y=1`.  
3. После выхода из процедуры:  
   - `a` остаётся `0` (так как `x` — параметр-значение).  
   - `b` стало `1`.  
   - Печать: `a=0 b=1`.  

**Вывод программы:**
```
x=1 y=1
a=0 b=1
```
